# Interview Question

Design accounts and balances where actual balance differs from available balance because of pending holds.

The interviewer is testing whether you can name source-of-truth rows, separate lifecycle state from read models, and prove correctness with constraints and verification queries instead of relying on one mutable field.
