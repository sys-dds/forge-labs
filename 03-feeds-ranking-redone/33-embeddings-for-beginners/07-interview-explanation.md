# Interview Explanation

Use hand-written user and item vectors to compute dot product and cosine similarity without ML libraries. A strong answer explains the matching rule with concrete item IDs and says which proof fails when retrieval cheats.
