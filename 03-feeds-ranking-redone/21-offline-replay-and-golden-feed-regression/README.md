# Offline Replay and Golden Feed Regression

Replay a fixed feed inventory and compare the derived actual feed with a golden feed before shipping.

Run the proof with `./scripts/forge-test-feeds-redone.sh 03-feeds-ranking-redone/21-offline-replay-and-golden-feed-regression`.
