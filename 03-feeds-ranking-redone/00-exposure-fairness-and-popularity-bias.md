# Exposure Fairness And Popularity Bias

Exposure budget is the row-level target for how much qualified supply should be shown. Popularity bias is what happens when historical engagement keeps feeding already-famous creators. In clinic 61, creator_ben has 900 shown events against a target of 300, exposure ratio 3.0; creator_maya has 0 historical exposure against 120, exposure ratio 0.0. Fairness here has an exposure example, not a slogan.

Long-tail content and exploration slot logic are fairness tools only when quality and audience fit pass. Debug by checking exposure_rows, fairness_rows, popularity_bias_rows, cold_start_rows, and downranked_items. A senior engineer asks whether outcome feedback later proves the exposure budget helped Ada and did not reward poor content.
