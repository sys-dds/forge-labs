# Scenario

Ada candidates become explicit feature rows before scoring so the ranker can be inspected.

The important behavior is deterministic: expected feature rows for [101,501,601,801,901] and unsafe Omar 701 excluded; broken likes-only scorer put 701 first and hid negative feedback.
