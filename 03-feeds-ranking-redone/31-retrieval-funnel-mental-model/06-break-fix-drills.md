# Break/Fix Drills

- Remove the source label for the first kept item and rerun the proof.
- Change one missed-candidate reason.
- Make `build_final_candidate_pool` return only IDs without debug trace evidence.
- Disable the exclusion used by a rejected item.
- Restore the retrieval rule and compare the trace.
