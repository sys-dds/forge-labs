# Debugging Notes

Start with `corpus`, then inspect `retrieval_sources`, `retrieved_candidates`, `missed_candidates`, `rejected_items`, and `retrieval_debug_trace`. The core function is `build_final_candidate_pool`.
