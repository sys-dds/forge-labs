# Scenario

Pure ranking gives Ada too many Ben backend posts, so a diversity pass builds the final page.

The important behavior is deterministic: expected diverse feed [101,102,601,701,901]; broken ranker returned unsafe 1001, repeated Ben four times, overfilled backend, or omitted exploration 901.
