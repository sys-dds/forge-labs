# Scenario

Build item-level explanations from candidate source, features, score, safety, and mixing traces.

Ada opens the feed. The clinic uses fixed rows so the learner can replay the same failure and corrected behavior repeatedly.
