# What To Notice

- Raw score is only one input.
- The corrected simulation emits rows that explain exposure budgets and popularity-bias control.
- The trace names IDs, stages, and reasons, so a reviewer can debug the ranking without guessing.
