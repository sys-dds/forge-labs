# Interview Explanation

Explain the failed backend invariant, then explain why the PostgreSQL fix is narrower than a broad application rewrite. Add JSONB shape checks and project typed values before filtering.
