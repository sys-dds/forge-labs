# Interview Explanation

Explain the failed backend invariant, then explain why the PostgreSQL fix is narrower than a broad application rewrite. Use `(actor, idem_key)` as the conflict target and avoid overwriting immutable payloads.
