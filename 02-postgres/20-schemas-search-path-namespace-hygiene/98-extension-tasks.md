# Extension Tasks

- Add one named row that should pass the rule and one named row that should fail it.
- Add a proof assertion that names the new row exactly.
- Explain whether the rule belongs in a constraint, view, function, trigger, policy, or caller query.
- Write a two-sentence interview answer that mentions the backend failure mode: public schema forever, unqualified security-sensitive names, search_path surprises.
