# Model Answer

I would start from the named row: Ben can reuse removed listing slug but not duplicate active slug. The PostgreSQL feature owns the boundary so a retry, admin script, or second service cannot bypass it. Senior signal: Scope uniqueness to seller and active rows. The trade-off is that the rule must stay documented near the schema or runner so future backend code understands why the database is rejecting or shaping the data.
