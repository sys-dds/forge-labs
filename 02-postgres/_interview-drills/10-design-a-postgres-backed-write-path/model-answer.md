# Model Answer

I would start from the named row: Resolve Cy dispute and write audit note together. The PostgreSQL feature owns the boundary so a retry, admin script, or second service cannot bypass it. Senior signal: Controlled mutation with exception and audit. The trade-off is that the rule must stay documented near the schema or runner so future backend code understands why the database is rejecting or shaping the data.
