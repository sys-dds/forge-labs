# Model Answer

I would start from the named row: Ben listing status change writes audit row. The PostgreSQL feature owns the boundary so a retry, admin script, or second service cannot bypass it. Senior signal: Mention hidden side effects and WHEN guard. The trade-off is that the rule must stay documented near the schema or runner so future backend code understands why the database is rejecting or shaping the data.
