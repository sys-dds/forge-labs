# Model Answer

I would start from the named row: Ada retries create-1 without duplicating a request. The PostgreSQL feature owns the boundary so a retry, admin script, or second service cannot bypass it. Senior signal: Mention immutable payload is not overwritten. The trade-off is that the rule must stay documented near the schema or runner so future backend code understands why the database is rejecting or shaping the data.
