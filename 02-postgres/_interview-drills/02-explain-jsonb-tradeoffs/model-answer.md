# Model Answer

I would start from the named row: Maya queries review_flagged payloads while actor stays typed. The PostgreSQL feature owns the boundary so a retry, admin script, or second service cannot bypass it. Senior signal: Separate core facts from flexible metadata. The trade-off is that the rule must stay documented near the schema or runner so future backend code understands why the database is rejecting or shaping the data.
