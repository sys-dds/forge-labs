# Model Answer

I would start from the named row: Alpha context sees Ada row, beta context sees Ben row. The PostgreSQL feature owns the boundary so a retry, admin script, or second service cannot bypass it. Senior signal: Mention app role and tenant setting. The trade-off is that the rule must stay documented near the schema or runner so future backend code understands why the database is rejecting or shaping the data.
