# PostgreSQL To Optimisation Readiness

Move to database optimisation when you can separate correctness from speed. You should be able to say which rows are correct before discussing indexes, plans, or latency. Bring proof tests from this path into optimisation work so faster queries still return the same rows.
