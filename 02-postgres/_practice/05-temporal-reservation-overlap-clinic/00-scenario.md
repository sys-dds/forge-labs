# Scenario

Ben accepts adjacent bookings but Ada cannot book an overlapping slot with the same provider.

Expected result: adjacent booking allowed; overlapping same provider rejected; different provider overlap allowed.
