# PostgreSQL Chapter Template

Copy this folder only when creating a real numbered chapter with named rows, runnable SQL, exact proof tests, break/fix tasks, and a PostgreSQL-specific backend reason.
