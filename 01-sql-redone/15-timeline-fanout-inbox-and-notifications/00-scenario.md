# Scenario

Ada's home feed reads a timeline table, and her notification badge reads an inbox table. The query must filter unsafe timeline rows, dedupe notifications by source event, and count only unread notification rows.

