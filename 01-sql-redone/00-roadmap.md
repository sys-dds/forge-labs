# Roadmap

SQL Redone stops at 15 clinics. The path now covers core SQL mechanics, endpoint-shaped backend queries, and product data patterns.

PostgreSQL Redone comes next for database-specific correctness: transactions, locking, indexes, isolation, migrations, and operational behavior.

A future Feed/Ranking Redone path can later teach ranking algorithms, personalization, diversity, experiments, and evaluation. That future path is named here as `03-feeds-ranking-redone/` only as a roadmap note.

Do not implement those future paths in this PR.
