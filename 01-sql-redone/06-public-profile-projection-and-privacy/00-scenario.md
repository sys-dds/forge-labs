# Scenario

Ada opens Ben's public profile. The API should return a shaped card, not `SELECT *` from profile tables. Ada also opens discovery search, where private profiles and blocked users must be excluded before the response is built.

