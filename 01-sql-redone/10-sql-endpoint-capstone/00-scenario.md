# Scenario

Ada's creator discovery endpoint should suggest creators reachable through people she follows. The response must exclude unsafe or already-known users, require visible public signal, and return stable cursor fields for pagination.

