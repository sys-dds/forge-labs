# What To Notice

- `NOT EXISTS` reads as "remove this author for this viewer."
- Ben post 102 is the zero-comment guard.
- The query is ordered only after the candidate set is correct.
- The proof checks exact ids, order, and counts instead of checking that "some rows" came back.

