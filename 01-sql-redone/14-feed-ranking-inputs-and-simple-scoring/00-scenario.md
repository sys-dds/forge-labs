# Scenario

Ada's candidate posts are already safe. The backend prepares a simple score from likes, comments, candidate source, and recency, then exposes the feature columns used to compute the score.

