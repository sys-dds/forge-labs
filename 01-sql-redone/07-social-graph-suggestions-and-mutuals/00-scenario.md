# Scenario

Ada asks for "People you may know." The backend can use second-degree outgoing edges, but must remove Ada, already-followed users, blocked users, and disconnected users before returning suggestions.

