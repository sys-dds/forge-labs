# Scenario

A dashboard asks how many signup users viewed the feed and created a first post. The raw events table has duplicates and internal traffic, so the query must turn events into user-level facts before counting.

