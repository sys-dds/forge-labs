# PostgreSQL Interview Prep

A strong answer names the row, the PostgreSQL feature, the app-only failure mode, and the trade-off. For example: Ada retries `create-1`; `(actor, idem_key)` prevents duplicate request rows while allowing Ben to reuse the same key in his own actor scope. The trade-off is that immutable payload rules must be documented and tested.
