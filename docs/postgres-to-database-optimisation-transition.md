# PostgreSQL To Database Optimisation Transition

Start optimisation after you can prove correctness across constraints, JSONB, views, functions, triggers, RLS, advisory locks, and schemas. Optimisation should preserve the proof tests from `02-postgres`; faster wrong rows are still wrong rows.
