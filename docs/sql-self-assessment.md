# SQL Self-Assessment

Self-assessment decides what to study next. Use the rubric levels, checklist, weak-spot map, and decision tree under `01-sql/_self-assessment/`.

```bash
./scripts/forge-list-self-assessment.sh
```

The practical question is: can you explain why exact rows appear or disappear, fix broken queries, and write proofs without weakening them?
