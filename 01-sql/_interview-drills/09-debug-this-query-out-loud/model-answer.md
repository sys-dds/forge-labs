# Model Answer

I would inspect the wrong rows first. Cy means the mute predicate is missing. Noor means the block predicate is missing. Ben post `101` with six joined rows means likes and comments are joined before aggregation. Ben post `102` missing means optional comments were treated as required.
