# Expected Results

`ada_mutual_matches`:

| matched_handle |
| --- |
| ben |

`ada_raw_candidates`:

| candidate_handle |
| --- |
| maya |
| omar |

`ada_preference_fit_candidates`:

| candidate_handle | shared_interest_count |
| --- | ---: |
| maya | 2 |

Cy liked Ada, but Ada passed on Cy, so there is no Ada-Cy match. Diya is blocked. Noor is inactive. Ben and Cy are already swiped. Ada is excluded from her own candidate set. Omar is raw-safe but outside Ada's age preference.
