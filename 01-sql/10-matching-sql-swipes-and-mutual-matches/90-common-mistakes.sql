-- Matching SQL, Swipes, and Mutual Matches: intentionally wrong patterns for study.
-- These statements are illustrative and are not run by the proof harness.
-- Read them after the working solution so the failure mode is easy to name.

-- Mistake 1: duplicate swipes allowed
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 2: self-swipe allowed
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 3: PASS treated as a match
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 4: already-swiped users included
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 5: blocked or inactive users reaching scoring
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 6: preference fit mixed unclearly with safety exclusions
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 7: ranking before exclusions
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.
