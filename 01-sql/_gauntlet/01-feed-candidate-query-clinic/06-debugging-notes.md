# Debugging Notes

Inspect `gauntlet_result` after loading the broken state. Post `101` has multiplied counts, post `102` is absent, and Cy/Noor remain eligible because the query filters follows but not safety rules.
