# Scenario

Ada opens her feed and sees Cy even though Cy is muted, plus Noor even though Noor is blocked. Ben post `102` is missing because it has no comments. Ben post `101` shows six engagement rows because two likes multiplied by three comments.
