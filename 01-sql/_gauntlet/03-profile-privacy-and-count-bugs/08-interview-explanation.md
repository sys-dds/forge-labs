# Interview Explanation

A public profile card is a shaped projection, not a table dump. I isolate post, follower, and following counts first so child tables do not multiply each other, then expose only safe fields.
