# Interview Explanation

I would traverse from Ada's outgoing follows to those users' outgoing follows, then subtract Ada, already-followed users, and blocked users. Diya survives; Noor is reachable but unsafe.
