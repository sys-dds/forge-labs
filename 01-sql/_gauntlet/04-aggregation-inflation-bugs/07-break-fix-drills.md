# Break/Fix Drills

- Replace isolated aggregates with `COUNT(*)` after joins.
- Remove `NOT hidden` and watch Ada's post count rise.
- Remove `NOT comments.deleted` and watch Ada's comment count rise.
