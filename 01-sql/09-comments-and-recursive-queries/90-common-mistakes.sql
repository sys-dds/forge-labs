-- Comments and Recursive Queries: intentionally wrong patterns for study.
-- These statements are illustrative and are not run by the proof harness.
-- Read them after the working solution so the failure mode is easy to name.

-- Mistake 1: recursive query without a clear base case
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 2: no parent foreign key
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 3: not scoping recursion to the post
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 4: unstable path ordering
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.

-- Mistake 5: no cycle strategy discussion
-- The authored dataset contains a row that would expose this bug during proof or manual inspection.
