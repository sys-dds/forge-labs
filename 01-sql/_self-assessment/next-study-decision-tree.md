# Next Study Decision Tree

If chapter proofs fail, stay in chapter mode.

If practice proofs fail, revisit the source chapter and rerun the related practice pack.

If gauntlets fail because the broken query still looks reasonable, inspect the named rows and write down which row exposes the bug.

If take-home proofs fail, compare your query grain against the model walkthrough.

If interview drills feel vague, answer again using exact rows: Ada, Ben, Cy, Noor, Diya, Maya, Omar, post `101`, post `102`, or cursor `106`.

Move toward PostgreSQL-specific study only when SQL failures are no longer about meaning, exclusions, counts, ordering, or proof shape.
