# Take-Home Format

Each take-home contains a brief, schema, seed data, candidate task, solution, proof tests, review rubric, follow-up questions, and model walkthrough.

The solution should be readable in one sitting and the proof should assert exact rows, counts, ordering, and exclusions.
