# Scaling Notes

Visibility can be precomputed later, but the query contract must keep owner, friendship, group membership, block, and deletion rules separate. Keep the source tables explainable even if a read model is materialized later.
