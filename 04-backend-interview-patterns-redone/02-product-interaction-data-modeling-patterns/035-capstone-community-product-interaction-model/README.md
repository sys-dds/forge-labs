# 035 Capstone Community Product Interaction Model

Combine memberships, roles, requests, invites, channels, posts, threads, moderation, events, RSVPs, waitlists, attendance, and audit trace in one small model.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/035-capstone-community-product-interaction-model
```

The proof loads base and variant datasets, runs the same verification query, and confirms the broken query executes but returns the wrong contract rows.
