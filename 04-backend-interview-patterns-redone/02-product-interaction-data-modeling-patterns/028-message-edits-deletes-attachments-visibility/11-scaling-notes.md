# Scaling Notes

Start with source correctness. Cache inbox or unread read models only after the source-derived contract is stable and replayable from `users`, `conversations`, `conversation_participants`, `messages`, `message_edits`.
