# API Shape

Return contract rows with `contract_name`, `subject_id`, `observed_value`, and `expected_reason`. A product API can map these rows to conversation details, inbox rows, unread badges, and communication safety explanations.
