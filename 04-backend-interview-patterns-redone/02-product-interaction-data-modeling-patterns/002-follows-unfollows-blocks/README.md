# BIP-PIM-002: Follows, unfollows, and blocks

What this clinic teaches: one-way follows, removed follows, block overrides, follower/following counts

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/002-follows-unfollows-blocks
```

Inspect `02-schema.sql`, `05-verification-query.sql`, and `07-broken-model-or-query.sql` to see how the proof catches the trap rows.
