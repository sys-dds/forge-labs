# 037 Search Queries Filters Facets Saved Searches

Model search query/session rows, filters, facets, saved searches, alert state, result visibility, blocks, and safety suppression.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/037-search-queries-filters-facets-saved-searches
```

The same verification query runs against base and variant data, then the broken query executes and proves the shortcut gives different rows.
