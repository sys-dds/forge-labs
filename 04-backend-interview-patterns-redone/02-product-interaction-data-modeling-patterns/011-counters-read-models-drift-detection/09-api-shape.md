# API Shape

Return contract fields that expose stable ids, lifecycle state, counts, source labels, and reason strings derived from `users, posts, post_reactions, comments, follow_edges, notifications, post_count_read_models, user_count_read_models, notification_count_read_models`. Do not expose raw joined tables as the product response.
