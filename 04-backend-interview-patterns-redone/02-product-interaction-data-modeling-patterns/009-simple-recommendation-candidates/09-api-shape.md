# API Shape

Expose compact ids, counts, state, source labels, and reason strings derived from `users, follow_edges, user_interests, creators, posts, swipe_events, matches, block_edges, report_events`.
