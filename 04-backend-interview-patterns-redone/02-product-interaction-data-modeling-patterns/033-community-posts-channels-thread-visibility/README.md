# 033 Community Posts Channels Thread Visibility

Model community posts, channel membership, threads, deleted/removed moderation state, and per-member visibility.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/033-community-posts-channels-thread-visibility
```

The proof loads base and variant datasets, runs the same verification query, and confirms the broken query executes but returns the wrong contract rows.
