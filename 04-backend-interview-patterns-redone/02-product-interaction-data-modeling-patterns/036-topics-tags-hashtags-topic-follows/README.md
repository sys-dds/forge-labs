# 036 Topics Tags Hashtags Topic Follows

Model topics, hashtags, content-topic links, creator-topic links, user topic follows, blocks, deleted content, and treatment traces.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/036-topics-tags-hashtags-topic-follows
```

The same verification query runs against base and variant data, then the broken query executes and proves the shortcut gives different rows.
