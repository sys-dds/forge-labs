# Scaling Notes

Stay small in this interview slice: model the source rows first, then discuss indexes and read models after correctness.

- Use read models only after source contracts are stable.
- Index join tables by viewer, topic, query, window, and collection.
- Keep debug traces explainable from component and treatment rows.

Avoid platform infrastructure detours for this clinic.
