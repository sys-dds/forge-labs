# 040 Capstone Discovery Search Collections Product Model

Combine topics, hashtags, search filters/facets, saved searches, trend components, safety suppression, collections, saved items, and discovery debug traces.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/040-capstone-discovery-search-collections-product-model
```

The same verification query runs against base and variant data, then the broken query executes and proves the shortcut gives different rows.
