# 031 Groups Communities Memberships Roles

Design communities with explicit membership lifecycle rows, role assignments, permissions, and bans.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/031-groups-communities-memberships-roles
```

The proof loads base and variant datasets, runs the same verification query, and confirms the broken query executes but returns the wrong contract rows.
