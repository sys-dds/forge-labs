# API Shape

Return contract rows with `contract_name`, `subject_id`, `observed_value`, and `expected_reason`. Product APIs can map those rows to marketplace feed, listing, seller, or booking explanations.
