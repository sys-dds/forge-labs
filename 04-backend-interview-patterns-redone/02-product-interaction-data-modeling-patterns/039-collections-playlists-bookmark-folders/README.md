# 039 Collections Playlists Bookmark Folders

Model collection ownership, visibility, members, ordered items, saved-item lifecycle, duplicate prevention, blocked authors, and collaboration.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/039-collections-playlists-bookmark-folders
```

The same verification query runs against base and variant data, then the broken query executes and proves the shortcut gives different rows.
