# 038 Trending Signals Time Windows Safety Suppression

Model trend windows, signal components, velocity, unique actors, freshness, spam evidence, policy treatments, snapshots, and debug traces.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/038-trending-signals-time-windows-safety-suppression
```

The same verification query runs against base and variant data, then the broken query executes and proves the shortcut gives different rows.
