# 034 Community Events RSVPs Attendance

Model community event visibility, RSVP lifecycle, waitlist promotion, attendance check-in, cancellation, and capacity.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/034-community-events-rsvps-attendance
```

The proof loads base and variant datasets, runs the same verification query, and confirms the broken query executes but returns the wrong contract rows.
