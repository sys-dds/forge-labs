# Scaling Notes

Suppression and review queues can be materialized, but reports and controls must remain auditable source rows. Controls stay viewer-specific unless a moderation workflow changes content state.
