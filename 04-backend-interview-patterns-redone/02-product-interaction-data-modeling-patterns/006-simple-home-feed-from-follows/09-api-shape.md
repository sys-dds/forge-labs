# API Shape

Return post ids, author ids, cursor fields, and reason labels from `users, follow_edges, block_edges, mute_edges, posts, feed_seen_events`. Keep raw global posts out of the home-feed response.
