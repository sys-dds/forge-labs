# 032 Join Requests Invites Private Communities

Model private access with join request lifecycle rows, invite lifecycle rows, bans, and request audit decisions.

Run:

```bash
./scripts/bip-pim-test-one.sh 04-backend-interview-patterns-redone/02-product-interaction-data-modeling-patterns/032-join-requests-invites-private-communities
```

The proof loads base and variant datasets, runs the same verification query, and confirms the broken query executes but returns the wrong contract rows.
