# Filtering, ordering, limits, and NULL traps

Scenario: Marketplace listings for a Glasgow buyer.

You will query `listings`. The important trap rows are 103 NULL price; 104 sold; 105 blocked; 106 and 108 tie by price and listing_id. The solution must pass both the base and variant seeds, so do not hard-code answer IDs.
