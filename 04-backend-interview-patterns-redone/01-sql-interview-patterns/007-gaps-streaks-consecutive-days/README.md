# Gaps, streaks, and consecutive-day problems

Scenario: Users practice SQL on different days.

You will query `users, practice_events`. The important trap rows are 101 duplicate same day; 101 skipped day then three-day streak; 104 active no events; 105 inactive excluded. The solution must pass both the base and variant seeds, so do not hard-code answer IDs.
