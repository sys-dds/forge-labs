# BIP-SQL-025: LATERAL joins for per-row top match

Practice JOIN LATERAL for top related row per parent on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/025-lateral-join-per-row-top-match`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
