# Question

For each buyer row, use LEFT JOIN LATERAL to rank only that buyer’s valid listings by budget fit, trust, distance, and listing id.

Return the exact columns shown in `06-expected-output.csv` and keep deterministic ordering. The same SQL must pass the base and variant seed.
