# BIP-SQL-026: Pivot-style conditional aggregation

Practice conditional aggregation to pivot event rows into metric columns on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/026-pivot-conditional-aggregation`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
