# BIP-SQL-027: Keyset pagination and stable cursors

Practice keyset pagination using a composite cursor on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/027-keyset-pagination-stable-cursors`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
