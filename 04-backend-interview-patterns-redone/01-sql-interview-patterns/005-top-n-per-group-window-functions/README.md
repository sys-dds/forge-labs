# Top-N per group with window functions

Scenario: Top posts per topic in a feed interview.

You will query `posts`. The important trap rows are 103 unsafe high score; 203 deleted high score; 101/102 tie by post_id. The solution must pass both the base and variant seeds, so do not hard-code answer IDs.
