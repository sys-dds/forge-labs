# BIP-SQL-030: Capstone senior SQL investigation query

Practice senior investigation query combining metrics, latest rows, reconciliation, transition debugging, and ratio safety on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/030-capstone-senior-sql-investigation-query`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
