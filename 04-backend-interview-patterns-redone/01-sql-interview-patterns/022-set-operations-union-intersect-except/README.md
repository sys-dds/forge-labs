# BIP-SQL-022: Set operations UNION INTERSECT EXCEPT

Practice UNION, INTERSECT, and EXCEPT set reasoning on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/022-set-operations-union-intersect-except`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
