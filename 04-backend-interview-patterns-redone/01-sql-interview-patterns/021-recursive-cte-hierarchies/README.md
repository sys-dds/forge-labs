# BIP-SQL-021: Recursive CTE hierarchies

Practice WITH RECURSIVE hierarchy traversal on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/021-recursive-cte-hierarchies`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
