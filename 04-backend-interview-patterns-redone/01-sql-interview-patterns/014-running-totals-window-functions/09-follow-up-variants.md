# Follow-Up Variants

- Ask how to include voided rows as visible zero-impact audit rows.
- Add a variant seed where a borderline row crosses exactly one threshold.
- Ask whether the output grain is one row per entity, event, date, session, or policy interval.
