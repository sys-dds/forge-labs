# SQL Debugging And Investigation Patterns

Investigation SQL should expose the reason a row is risky: latest moderation, invalid transitions, ledger gaps, unsafe ratios, and low conversion all become visible columns. Clinic 030 combines those signals into one audit-friendly report.
