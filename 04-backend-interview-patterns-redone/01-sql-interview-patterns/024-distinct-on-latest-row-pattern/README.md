# BIP-SQL-024: PostgreSQL DISTINCT ON latest row

Practice PostgreSQL DISTINCT ON for latest-row-per-entity on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/024-distinct-on-latest-row-pattern`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
