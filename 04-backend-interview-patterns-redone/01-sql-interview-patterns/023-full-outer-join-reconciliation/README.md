# BIP-SQL-023: FULL OUTER JOIN reconciliation

Practice FULL OUTER JOIN reconciliation between systems on concrete marketplace interview data. Run `./scripts/bip-sql-test-one.sh 04-backend-interview-patterns-redone/01-sql-interview-patterns/023-full-outer-join-reconciliation`.

The base and variant datasets both use the same `05-solution.sql`, so a query shaped around one fixed answer will fail.
